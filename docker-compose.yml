services:

  # ── Database ────────────────────────────────────────────────────────────────
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=${SA_PASSWORD:-MediPulse@2024!Dev}
      - MSSQL_PID=Developer
    ports:
      - "1433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -C -S localhost -U sa -P '${SA_PASSWORD:-MediPulse@2024!Dev}' -Q 'SELECT 1' -b -No"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks:
      - medipulse

  # ── Migration Service ───────────────────────────────────────────────────────
  migrator:
    build: .
    depends_on:
      sqlserver:
        condition: service_healthy
    volumes:
      - .:/src
    working_dir: /src
    entrypoint: ["/bin/sh", "-c"]
    command: |
      set -eu
      echo "Waiting for SQL Server to be ready..."
      # Wait for SQL Server to be ready
      i=1
      while [ "$$i" -le 30 ]; do
        if /opt/mssql-tools18/bin/sqlcmd -C -b -S sqlserver -U SA -P "${SA_PASSWORD:-MediPulse@2024!Dev}" -Q "SELECT 1" 2>/dev/null; then
          echo "SQL Server is ready"
          break
        fi
        echo "Waiting for SQL Server to start..."
        sleep 2
        i=$$(($$i + 1))
      done

      echo "Running EF Core migrations for all services..."
      MAIN_CONNECTION="Server=sqlserver,1433;Database=MedipulseMain;User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;"
      AUDIT_CONNECTION="Server=sqlserver,1433;Database=MedipulseAudit;User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;"
      dotnet ef database update --project AuthService/AuthService.csproj --connection "$$MAIN_CONNECTION"
      dotnet ef database update --project FacilityService/FacilityService.csproj --connection "$$MAIN_CONNECTION"
      dotnet ef database update --project InventoryService/InventoryService.csproj --connection "$$MAIN_CONNECTION"
      dotnet ef database update --project ProcurementService/ProcurementService.csproj --connection "$$MAIN_CONNECTION"
      dotnet ef database update --project LogisticsService/LogisticsService.csproj --connection "$$MAIN_CONNECTION"
      dotnet ef database update --project TelemetryService/TelemetryService.csproj --connection "$$MAIN_CONNECTION"
      dotnet ef database update --project NotificationService/NotificationService.csproj --connection "$$MAIN_CONNECTION"
      dotnet ef database update --project AuditService/AuditService.csproj --connection "$$AUDIT_CONNECTION"

      echo "Loading mock data..."
      /opt/mssql-tools18/bin/sqlcmd -C -S sqlserver -U SA -P "${SA_PASSWORD:-MediPulse@2024!Dev}" -i MockData_InsertOnly.sql -d MedipulseMain

      echo "All migrations and data loading completed successfully!"
    networks:
      - medipulse

  # ── Backend services ─────────────────────────────────────────────────────────
  authservice:
    build:
      context: .
      dockerfile: AuthService/Dockerfile
    environment:
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=${DB_NAME_MAIN:-MedipulseMain};User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;
      - ConnectionStrings__AuditConnection=Server=sqlserver,1433;Database=${DB_NAME_AUDIT:-MedipulseAudit};User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;
      - Jwt__Key=${JWT_KEY:-MediPulse@SuperSecretJWT_Key_2024!ForDev}
      - Jwt__Issuer=${JWT_ISSUER:-MediPulseAuthService}
      - Jwt__Audience=${JWT_AUDIENCE:-MediPulseAPI}
      - AuditService__BaseUrl=http://auditservice:8080
      - ServiceName=AuthService
    depends_on:
      migrator:
        condition: service_completed_successfully
    networks:
      - medipulse

  facilityservice:
    build:
      context: .
      dockerfile: FacilityService/Dockerfile
    environment:
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=${DB_NAME_MAIN:-MedipulseMain};User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;
      - ConnectionStrings__AuditConnection=Server=sqlserver,1433;Database=${DB_NAME_AUDIT:-MedipulseAudit};User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;
      - Jwt__Key=${JWT_KEY:-MediPulse@SuperSecretJWT_Key_2024!ForDev}
      - Jwt__Issuer=${JWT_ISSUER:-MediPulse}
      - Jwt__Audience=${JWT_AUDIENCE:-MediPulseUsers}
      - AuditService__BaseUrl=http://auditservice:8080
      - ServiceName=FacilityService
    depends_on:
      migrator:
        condition: service_completed_successfully
    networks:
      - medipulse

  inventoryservice:
    build:
      context: .
      dockerfile: InventoryService/Dockerfile
    environment:
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=${DB_NAME_MAIN:-MedipulseMain};User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;
      - ConnectionStrings__AuditConnection=Server=sqlserver,1433;Database=${DB_NAME_AUDIT:-MedipulseAudit};User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;
      - Jwt__Key=${JWT_KEY:-MediPulse@SuperSecretJWT_Key_2024!ForDev}
      - Jwt__Issuer=${JWT_ISSUER:-MediPulse}
      - Jwt__Audience=${JWT_AUDIENCE:-MediPulseUsers}
      - AuditService__BaseUrl=http://auditservice:8080
      - NotificationService__BaseUrl=http://notificationservice:8080
      - ServiceName=InventoryService
    depends_on:
      migrator:
        condition: service_completed_successfully
    networks:
      - medipulse

  procurementservice:
    build:
      context: .
      dockerfile: ProcurementService/Dockerfile
    environment:
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=${DB_NAME_MAIN:-MedipulseMain};User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;
      - ConnectionStrings__AuditConnection=Server=sqlserver,1433;Database=${DB_NAME_AUDIT:-MedipulseAudit};User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;
      - Jwt__Key=${JWT_KEY:-MediPulse@SuperSecretJWT_Key_2024!ForDev}
      - Jwt__Issuer=${JWT_ISSUER:-MediPulse}
      - Jwt__Audience=${JWT_AUDIENCE:-MediPulseUsers}
      - AuditService__BaseUrl=http://auditservice:8080
      - NotificationService__BaseUrl=http://notificationservice:8080
      - ServiceName=ProcurementService
    depends_on:
      migrator:
        condition: service_completed_successfully
    networks:
      - medipulse

  logisticsservice:
    build:
      context: .
      dockerfile: LogisticsService/Dockerfile
    environment:
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=${DB_NAME_MAIN:-MedipulseMain};User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;
      - ConnectionStrings__AuditConnection=Server=sqlserver,1433;Database=${DB_NAME_AUDIT:-MedipulseAudit};User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;
      - Jwt__Key=${JWT_KEY:-MediPulse@SuperSecretJWT_Key_2024!ForDev}
      - Jwt__Issuer=${JWT_ISSUER:-MediPulse}
      - Jwt__Audience=${JWT_AUDIENCE:-MediPulseUsers}
      - AuditService__BaseUrl=http://auditservice:8080
      - NotificationService__BaseUrl=http://notificationservice:8080
      - ServiceName=LogisticsService
    depends_on:
      migrator:
        condition: service_completed_successfully
    networks:
      - medipulse

  telemetryservice:
    build:
      context: .
      dockerfile: TelemetryService/Dockerfile
    environment:
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=${DB_NAME_MAIN:-MedipulseMain};User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;
      - ConnectionStrings__AuditConnection=Server=sqlserver,1433;Database=${DB_NAME_AUDIT:-MedipulseAudit};User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;
      - Jwt__Key=${JWT_KEY:-MediPulse@SuperSecretJWT_Key_2024!ForDev}
      - Jwt__Issuer=${JWT_ISSUER:-MediPulse}
      - Jwt__Audience=${JWT_AUDIENCE:-MediPulseUsers}
      - AuditService__BaseUrl=http://auditservice:8080
      - ServiceName=TelemetryService
    depends_on:
      migrator:
        condition: service_completed_successfully
    networks:
      - medipulse

  notificationservice:
    build:
      context: .
      dockerfile: NotificationService/Dockerfile
    environment:
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=${DB_NAME_MAIN:-MedipulseMain};User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;
      - ConnectionStrings__AuditConnection=Server=sqlserver,1433;Database=${DB_NAME_AUDIT:-MedipulseAudit};User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;
      - Jwt__Key=${JWT_KEY:-MediPulse@SuperSecretJWT_Key_2024!ForDev}
      - Jwt__Issuer=${JWT_ISSUER:-MediPulse}
      - Jwt__Audience=${JWT_AUDIENCE:-MediPulseUsers}
      - AuditService__BaseUrl=http://auditservice:8080
      - ServiceName=NotificationService
    depends_on:
      migrator:
        condition: service_completed_successfully
    networks:
      - medipulse

  auditservice:
    build:
      context: .
      dockerfile: AuditService/Dockerfile
    environment:
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=${DB_NAME_MAIN:-MedipulseMain};User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;
      - ConnectionStrings__AuditConnection=Server=sqlserver,1433;Database=${DB_NAME_AUDIT:-MedipulseAudit};User Id=sa;Password=${SA_PASSWORD:-MediPulse@2024!Dev};TrustServerCertificate=True;
      - Jwt__Key=${JWT_KEY:-MediPulse@SuperSecretJWT_Key_2024!ForDev}
      - Jwt__Issuer=${JWT_ISSUER:-MediPulse}
      - Jwt__Audience=${JWT_AUDIENCE:-MediPulseUsers}
      - ServiceName=AuditService
    depends_on:
      migrator:
        condition: service_completed_successfully
    networks:
      - medipulse

  # ── Gateway ──────────────────────────────────────────────────────────────────
  gateway:
    build:
      context: .
      dockerfile: Gateway/Dockerfile
    ports:
      - "5000:8080"
    depends_on:
      - authservice
      - facilityservice
      - inventoryservice
      - procurementservice
      - logisticsservice
      - telemetryservice
      - notificationservice
      - auditservice
    networks:
      - medipulse

  # ── Frontend (Angular → nginx) ───────────────────────────────────────────────
  frontend:
    build:
      context: .
      dockerfile: Frontend/Dockerfile
    ports:
      - "80:80"
    depends_on:
      - gateway
    networks:
      - medipulse

  # ── SonarQube Code Analyzer ─────────────────────────────────────────────────
  sonarqube:
    image: sonarqube:lts-community
    ports:
      - "9000:9000"
    environment:
      - SONAR_ES_BOOTSTRAP_CHECKS_DISABLE=true
    volumes:
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_extensions:/opt/sonarqube/extensions
      - sonarqube_logs:/opt/sonarqube/logs
    networks:
      - medipulse

networks:
  medipulse:
    driver: bridge

volumes:
  sqlserver_data:
  sonarqube_data:
  sonarqube_extensions:
  sonarqube_logs:
